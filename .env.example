# IMPORTANT: Change all values below for production!
# Generate secure JWT_SECRET: openssl rand -base64 32
# Get Google OAuth credentials from: https://console.cloud.google.com

NODE_ENV=production
PORT=8000
JWT_SECRET=change-this-to-secure-random-string
DATABASE_URL=postgresql://user:password@host:5432/database

# Google OAuth Configuration
# Make sure to configure authorized redirect URIs in Google Cloud Console:
# - https://knight.lawrenceallen.tech/auth/google/callback
GOOGLE_CLIENT_ID=your-production-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-production-secret
GOOGLE_REDIRECT_URI=https://knight.lawrenceallen.tech/auth/google/callback
